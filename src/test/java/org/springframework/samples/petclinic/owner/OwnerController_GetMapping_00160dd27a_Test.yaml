# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: 'http://dummyapi.com'
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: 'Successful API request'
    flow:
      - get:
          url: "/owners/find"
          expect:
            - statusCode: 200
            - hasProperty: 'owners/findOwners'

  - name: 'API request with validation failure'
    flow:
      - get:
          url: "/owners/find"
          headers:
            invalidHeader: 'invalidValue'
          expect:
            - statusCode: 400

  - name: 'API request with edge case'
    flow:
      - get:
          url: "/owners/find"
          headers:
            edgeCaseHeader: 'specialCharactersOrLargeData'
          expect:
            - statusCode: 
                js: |
                  function(statusCode) {
                    if (statusCode >= 200 && statusCode < 300) {
                      return true;
                    } else {
                      return false;
                    }
                  }
            - hasProperty: 'owners/findOwners'

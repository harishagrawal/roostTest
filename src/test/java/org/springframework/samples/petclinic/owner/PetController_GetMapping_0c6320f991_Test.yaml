# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            # Add more assertions based on the expected response body

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            # Add more assertions based on the expected error message

  - name: "API request with edge case"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Content-Type: "application/json"
          expect:
            # Assert that the status code is within an acceptable range
            - statusCode:
                inRange: [200, 500]
            # Add more assertions based on the expected pattern or behavior

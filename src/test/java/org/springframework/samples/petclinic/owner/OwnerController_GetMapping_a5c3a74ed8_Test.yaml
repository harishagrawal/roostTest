# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/owners/new"
          expect:
            - statusCode: 200
            - hasProperty: "owner"
  
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/owners/new"
          json:
            owner: ""
          expect:
            - statusCode: 400
            - equals:
                - message: "Owner field is required"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/owners/new"
          json:
            owner: "!@#$%^&*()_+{}|:\"?><~`"
          expect:
            - statusCode: 200
            - equals:
                - message: "Special characters are not allowed in owner field"

# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Authorization: "Bearer token"
          expect:
            - statusCode: 200
            - hasProperty: "pet"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Authorization: "Bearer token"
          qs:
            owner: "" # Missing required field
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/pets/new"
          headers:
            Authorization: "Bearer token"
          qs:
            owner: "A string with special characters !@#$%^&*()" # Special characters
          expect:
            - statusCode: 200
            - hasProperty: "pet"

# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: 'http://localhost:8080'
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: 'Successful API request'
    flow:
      - get:
          url: "/pets/new"
          headers:
            Owner: "John Doe"
            ModelMap: "model"
          expect:
            - statusCode: 200
            - hasProperty: "pet"

  - name: 'API request with validation failure'
    flow:
      - get:
          url: "/pets/new"
          headers:
            Owner: ""
            ModelMap: "model"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: 'API request with edge case'
    flow:
      - get:
          url: "/pets/new"
          headers:
            Owner: "John Doe"
            ModelMap: "model"
          expect:
            - statusCode: 200
            - hasProperty: "pet"
            - equals: 
                pet: "SpecialCharacterPetName"

# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/pets/{{ petId }}/edit"
          headers:
            Authorization: "Bearer {{ token }}"
          capture:
            - json: "$.pet"
              as: "pet"
          expect:
            - statusCode: 200
            - equals:
                - "{{ pet }}"
                - "{{ expectedPetData }}"
        # This test checks if a valid request returns a 200 status code and the correct pet data.

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/pets/{{ petId }}/edit"
          headers:
            Authorization: "Bearer {{ token }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        # This test checks if an invalid request (e.g. missing required fields) returns a 400 status code and an error message.

  - name: "API request with edge case"
    flow:
      - get:
          url: "/pets/{{ petId }}/edit"
          headers:
            Authorization: "Bearer {{ token }}"
          capture:
            - json: "$.pet"
              as: "pet"
          expect:
            - statusCode: 200
            - equals:
                - "{{ pet }}"
                - "{{ expectedPetData }}"
        # This test checks if a request with special characters or large data returns a 200 status code and the correct pet data.

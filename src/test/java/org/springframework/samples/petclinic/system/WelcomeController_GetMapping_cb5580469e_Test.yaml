# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get: "/"
        expect:
          - statusCode: 200
          - body: "welcome"

  - name: "API request with invalid endpoint"
    flow:
      - get: "/invalid"
        expect:
          - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get: "/?param=%20%22%3C%3E%7B%7D%5B%5D%60~!@#$%^&*()"
        expect:
          - statusCode: 400

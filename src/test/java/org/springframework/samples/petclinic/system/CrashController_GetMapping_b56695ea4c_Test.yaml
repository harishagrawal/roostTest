# Test generated by RoostGPT for test roostTest-API using AI Type Open AI and AI Model gpt-4

config:
  target: "http://your-api-endpoint.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/oups"
          expect:
            - statusCode: 200
            - hasProperty: 'Expected: controller used to showcase what happens when an exception is thrown'

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/oups"
          headers:
            invalid_header: "invalid_value"
          expect:
            - statusCode: 400
            - hasProperty: 'error'

  - name: "API request with edge case"
    flow:
      - get:
          url: "/oups"
          headers:
            special_header: "!@#$%^&*()"
          expect:
            - statusCode: 200
            - hasProperty: 'Expected: controller used to showcase what happens when an exception is thrown'
